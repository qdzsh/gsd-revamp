{
  "_comment": "Allowlist of stdio MCP servers to auto-trust without per-session approval prompt. Match by server name (key in the user-level ~/.gsd/mcp.json mcpServers map). Repo-local MCP configs are never auto-trusted by this file. HTTP servers (e.g. linkup) bypass the trust check at the transport layer.",
  "servers": [
    "linkup",
    "ctxe",
    "context7"
  ]
}
